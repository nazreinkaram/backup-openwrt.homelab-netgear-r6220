# Environment file for sync-git, sourcing this file will provide the
# variable and functions needed to sync configuration files with git.
export SYNC_GIT_SHELL_PROFILE="../.sync-git-shell-profile"

source "$SYNC_GIT_SHELL_PROFILE"
